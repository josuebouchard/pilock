{% from "components/macros.jinja" import input_field, form_error_box, breadcrumb %}
{% extends "base.jinja" %}
{% block title %}Access Logs - Download{% endblock title %}

{% block body %}
<div class="mx-auto container ">
    <h1 class="text-4xl font-bold my-4">Download access logs information</h1>

    {{
    breadcrumb(
    links=[{"url": request.url_for('logs:index-ui'), "label": "Logs"}, ],
    current="Download",
    )
    }}

    <div class="bg-neutral-50 rounded-md shadow-md">
        <form action="#" method="post" class="p-6 space-y-6" novalidate>
            {% if csrf_token %}<input type="hidden" name="csrf_token" value="{{ csrf_token }}">{% endif %}

            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                {{ input_field('from_date', 'From date', type="date", form=form, errors=errors, required=True) }}
                {{ input_field('to_date', 'To date', type="date", form=form, errors=errors, required=True) }}
            </div>

            {{ form_error_box(errors) }}

            <div class="flex items-center gap-3">
                <button type="submit" class="inline-flex items-center rounded-md bg-purple-600 px-4 py-2 font-medium text-neutral-50
                 hover:bg-purple-700 focus:outline-none focus:ring-2 focus:ring-purple-500">
                    Download
                </button>

                <a href="{{url_for('logs:index-ui')}}" class="inline-flex items-center rounded-md border border-neutral-300 bg-white px-4 py-2 font-medium
                 text-neutral-700 hover:bg-neutral-100 focus:outline-none focus:ring-2 focus:ring-neutral-400">
                    Cancel
                </a>
            </div>
        </form>
    </div>
</div>
{% endblock body %}